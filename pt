#!/bin/bash
# Project Tracker launcher script
# 🚀 Usage: ./pt [command]
# Example: ./pt launch
# (Note: This script automatically uses the venv, but for manual script 
#  running, use: source venv/bin/activate)

cd "$(dirname "$0")"
exec venv/bin/python scripts/pt.py "$@"

